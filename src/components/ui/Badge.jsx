export default function Badge({ children }) {
  return <span className="inline-flex items-center rounded-full border border-[#e3e1dc] bg-[#ffffff] px-2.5 py-0.5 font-mono text-[10px] font-medium uppercase tracking-[0.15em] text-[#1a1a1a]">{children}</span>;
}
